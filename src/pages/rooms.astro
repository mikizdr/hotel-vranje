---
import Page from '../components/Page.astro';
import { getDictionary } from '../i18n';

const dict = getDictionary('sr');
const { about } = dict.pages;
---

<Page dict={dict} pageKey="rooms" lang="sr">
    <p>Sobe</p>
</Page>
