---
import Page from '../../components/Page.astro';
import { getDictionary } from '../../i18n';

const dict = getDictionary('en');
const { contact } = dict.pages;
---

<Page dict={dict} pageKey="contact" lang="en">
    <p>Contact</p>
</Page>
